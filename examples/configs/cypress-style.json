{
  "_comment": "Cypress-first setup. Emits `data-cy` instead of `data-testid`, uses a short prefix-based idFormat (`cy-component-element-key`) for readable selectors in Cypress specs, and keeps the registry compact while still capturing the high-signal semantic fields that make IDs stable across template edits.",
  "tagger": {
    "rootDir": "src",
    "include": ["**/*.component.html"],
    "registryDir": "test-artifacts/cypress-ids",
    "attributeName": "data-cy",
    "idFormat": "cy-{component}-{element}-{key}",
    "hashLength": 4,
    "testConfigurationOnly": false,
    "registry": {
      "profile": "minimal",
      "includeSemantics": true,
      "semanticFields": [
        "formcontrolname",
        "aria_label",
        "text_content",
        "label",
        "event_handlers",
        "i18n_keys",
        "context"
      ]
    }
  },
  "locators": {
    "attributeName": "data-cy",
    "variableFormat": "{component}_{element}_{key}",
    "xpathPrefix": ""
  }
}
